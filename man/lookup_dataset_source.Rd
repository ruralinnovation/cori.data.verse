% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup_dataset_source.R
\name{lookup_dataset_source}
\alias{lookup_dataset_source}
\title{Look up canonical (source, sourceUrl) for a vector of dataset slugs.}
\usage{
lookup_dataset_source(
  slugs,
  s3_bucket = "cori.data.verse",
  s3_prefix = "dev/content/"
)
}
\arguments{
\item{slugs}{Character vector of dataset slugs.}

\item{s3_bucket}{Character. Default \code{"cori.data.verse"}.}

\item{s3_prefix}{Character. S3 key prefix. Default \code{"dev/content/"}.}
}
\value{
List with \verb{$source} and \verb{$sourceUrl}, or \code{NULL} if no match.
}
\description{
Reads dataset profiles from S3 (cached in-process via
\code{fetch_dataset_profiles_from_s3()}). Returns a list with \verb{$source} and
\verb{$sourceUrl} derived from the first matching slug; \code{NULL} if no slug
matches or S3 is unreachable.
}
\details{
When multiple slugs are supplied and more than one is recognized, the
first recognized slug wins. (\code{generate_chart_qmd()} calls this with the
chart's \code{usesDatasets} vector, where the first slug is the primary
dependency.)
}
