% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_chart_qmd.R
\name{generate_chart_frontmatter}
\alias{generate_chart_frontmatter}
\title{Generate Chart YAML Frontmatter}
\usage{
generate_chart_frontmatter(
  title,
  description,
  slug,
  date = Sys.Date(),
  categories = character(0),
  tags = character(0),
  image = NULL,
  chart_type = NULL,
  interactive = FALSE,
  data_source = NULL,
  source_url = NULL,
  featured = FALSE,
  produced_by = NULL,
  uses_datasets = character(0),
  uses_packages = character(0),
  uses_resources = character(0)
)
}
\arguments{
\item{title}{Character.}

\item{description}{Character.}

\item{slug}{Character. URL slug for the chart (kebab-case, globally unique).}

\item{date}{Character or Date. Defaults to \code{Sys.Date()}.}

\item{categories}{Character vector.}

\item{tags}{Character vector.}

\item{image}{Character or \code{NULL}. Cover/thumbnail image filename relative
to the chart directory (e.g., \code{"consumer_sentiment_lc.svg"}).}

\item{chart_type}{Character or \code{NULL}. Logical chart type
(\code{"line"}, \code{"bar"}, \code{"map"}, \code{"density"}, \code{"scatter"}, \code{"network"}, ...).}

\item{interactive}{Logical. Defaults to \code{FALSE}.}

\item{data_source}{Character or \code{NULL}. Human-readable data source name
(e.g., \code{"Quarterly Census of Employment and Wages"}).}

\item{source_url}{Character or \code{NULL}. URL to the upstream data source.}

\item{featured}{Logical. Defaults to \code{FALSE}.}

\item{produced_by}{Character or \code{NULL}. Slug of the project that produced
the chart. Maps to \code{producedBy} in the YAML.}

\item{uses_datasets}{Character vector. Dataset slugs (kebab-case).}

\item{uses_packages}{Character vector. Package slugs.}

\item{uses_resources}{Character vector. Resource slugs.}
}
\value{
Character string of YAML (with \verb{---} delimiters and a trailing
newline).
}
\description{
Builds a YAML frontmatter block conforming to the \code{ChartMeta} interface
defined in \code{src/types/index.ts}. Includes the nested \verb{chart:} block and the
graph-edge fields (\code{producedBy}, \code{usesDatasets}, \code{usesPackages},
\code{usesResources}) compatible with \code{build_graph()}. All slugs are preserved
(valid + dangling).
}
