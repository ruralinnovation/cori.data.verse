% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_project_qmd.R
\name{validate_project_dependencies}
\alias{validate_project_dependencies}
\title{Validate Project Dependencies Against Dataverse Content (Local + S3)}
\usage{
validate_project_dependencies(
  uses_datasets = character(0),
  uses_packages = character(0),
  uses_resources = character(0),
  dataverse_root = here::here(),
  s3_bucket = "cori.data.verse",
  s3_prefix = NULL,
  check_s3 = TRUE
)
}
\arguments{
\item{uses_datasets}{Character vector of dataset slugs (kebab-case).}

\item{uses_packages}{Character vector of package slugs.}

\item{uses_resources}{Character vector of resource slugs.}

\item{dataverse_root}{Character. Root of Dataverse project. Defaults to
\code{here::here()}.}

\item{s3_bucket}{Character. Default \code{"cori.data.verse"}.}

\item{s3_prefix}{Character or \code{NULL}. If \code{NULL}, auto-detects from current
git branch.}

\item{check_s3}{Logical. Default \code{TRUE}. Set \code{FALSE} to skip S3.}
}
\value{
A list with three per-type partitions plus context:
\describe{
\item{datasets}{\code{list(valid_local, valid_remote, dangling)}}
\item{packages}{\code{list(valid_local, valid_remote, dangling)}}
\item{resources}{\code{list(valid_local, valid_remote, dangling)}}
\item{s3_prefix_used}{The prefix queried, or \code{NA} if skipped.}
\item{summary}{Character. Human-readable report.}
}
}
\description{
Compares inferred dataset/package/resource slugs against existing Dataverse
content in two locations: the local content directory and the remote S3
prefix (\verb{s3://cori.data.verse/\{branch\}/content/}). A slug present in either
location is "valid"; one missing from both is "dangling." Dangling
references are intentionally preserved in the caller's slug vectors so
they surface as future content needs in the graph.
}
