% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discover_chart_outputs.R
\name{discover_chart_outputs}
\alias{discover_chart_outputs}
\title{Discover Chart Outputs in a Project}
\usage{
discover_chart_outputs(
  project_dir = getwd(),
  project_slug,
  git_only = TRUE,
  image_subdirs = "export"
)
}
\arguments{
\item{project_dir}{Project root (default: \code{getwd()}).}

\item{project_slug}{Character. Slug of the producing project, used as a
prefix for each chart's slug. Required.}

\item{git_only}{Logical. If \code{TRUE} (default), restrict to scripts under
\verb{git ls-files}. Untracked scripts are skipped to keep the catalog
reproducible.}

\item{image_subdirs}{Character vector. Subdirectories of \code{project_dir} to
search for actual image files. Searched recursively. Default \code{"export"}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{script_path}{Relative path to the R script}
\item{image_basename}{Filename stem, no extension
(e.g. \code{"consumer_sentiment_lc"})}
\item{image_format}{\code{"svg"} or \code{"png"} -- whichever exists, SVG
preferred. \code{NA} if the declared image is not on disk.}
\item{image_path}{Relative path to the actual image, or \code{NA}}
\item{slug}{Suggested chart slug:
\code{{project_slug}-{normalized-basename}}}
\item{labs_title}{First \code{labs(title=...)} value found in the script,
or \code{NA}}
\item{labs_subtitle}{First \code{labs(subtitle=...)} value, or \code{NA}}
\item{labs_caption}{First \code{labs(caption=...)} value, or \code{NA}}
}
}
\description{
Scans git-tracked R scripts in a project for ggplot save calls
(\code{cori.charts::save_plot}, \code{save_plot}, \code{ggsave}) and matches the declared
output paths against actual image files on disk. Returns one row per
distinct (script, image basename) pair, preferring SVG over PNG.
}
\details{
Use this from a Claude Code skill (\verb{/generate-dataverse-charts}) before
generating per-chart \code{index.qmd} files. The caller is expected to read the
producing project's slug from its own \code{index.qmd} and pass it via
\code{project_slug} so chart slugs can be globally namespaced
(e.g., \code{rural-economic-outlook-emp-change-since-gr-lc}).
}
