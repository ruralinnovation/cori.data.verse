% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_metadata_sidecars.R
\name{write_metadata_sidecars}
\alias{write_metadata_sidecars}
\title{Write metadata sidecars for every rendered .md file in \verb{content/}.}
\usage{
write_metadata_sidecars(root_dir = here::here(), content_dir = "content")
}
\arguments{
\item{root_dir}{Project root containing both source \code{.qmd} directories
(\verb{datasets/}, \verb{charts/}, \verb{packages/}, \verb{projects/}, \verb{posts/},
\verb{resources/}) AND the rendered \verb{content/} tree. Defaults to
\code{\link[here:here]{here::here()}}.}

\item{content_dir}{Subdirectory of \code{root_dir} containing the rendered
\code{.md} output. Defaults to \code{"content"}.}
}
\value{
Invisibly returns the character vector of sidecar paths written
(relative to \code{root_dir}).
}
\description{
Walks the rendered content tree and, for each \verb{<type>/<slug>/index.md},
parses the source \verb{<type>/<slug>/index.qmd}'s YAML frontmatter (via
\code{\link[=read_qmd_frontmatter]{read_qmd_frontmatter()}}) and writes the parsed object as JSON to
\verb{<type>/<slug>/index.md.metadata.json}. The sidecar contains the full
frontmatter — every field, including nested blocks like \verb{dataset:} and
\verb{chart:} — so downstream consumers can read structured metadata that
would otherwise be lost during Quarto's GFM render.
}
\details{
Idempotent: re-running on an unchanged repo regenerates byte-identical
sidecars (within JSON serialization variability).

Project-root rendered files (\code{content/index.md}, \code{content/about.md})
are also handled: they read frontmatter from \code{index.qmd} / \code{about.qmd}
at the project root.
}
