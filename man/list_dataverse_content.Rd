% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_project_qmd.R
\name{list_dataverse_content}
\alias{list_dataverse_content}
\title{List All Dataverse Content Nodes (Local + S3)}
\usage{
list_dataverse_content(
  dataverse_root = here::here(),
  s3_bucket = "cori.data.verse",
  s3_prefix = NULL,
  check_s3 = TRUE
)
}
\arguments{
\item{dataverse_root}{Character. Root of the Dataverse project. Defaults to
\code{here::here()}.}

\item{s3_bucket}{Character. Default \code{"cori.data.verse"}.}

\item{s3_prefix}{Character or \code{NULL}. If \code{NULL}, auto-detects via the
internal \code{detect_s3_prefix()} (branch-aware: \code{"main/content/"} on main,
else \code{"dev/content/"}).}

\item{check_s3}{Logical. Default \code{TRUE}. Set \code{FALSE} to skip S3.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{type}{Character. One of \code{"dataset"}, \code{"chart"}, \code{"package"},
\code{"project"}, \code{"resource"}, \code{"post"}.}
\item{slug}{Character. URL slug (kebab-case directory name).}
\item{location}{Character. \code{"local"}, \code{"remote"}, or \code{"both"}.}
\item{local_path}{Character or \code{NA}. Path to local index.qmd if present.}
\item{s3_key}{Character or \code{NA}. S3 key if present.}
}
Sorted by type, then slug. Includes attribute \code{s3_prefix_used}.
}
\description{
Enumerates every content item across the local content directories and the
remote S3 prefix, returning a unified inventory keyed by type and slug.
Each item is tagged with its location(s): "local", "remote", or "both".
}
\details{
Use this before slug mapping/validation so the caller can pick existing
slugs by reuse, spot near-matches, and decide which discovered dependencies
will be dangling.
}
