% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_project_qmd.R
\name{list_dataverse_content}
\alias{list_dataverse_content}
\title{List All Dataverse Content Nodes from S3}
\usage{
list_dataverse_content(
  s3_bucket = "cori.data.verse",
  s3_prefix = "dev/content/"
)
}
\arguments{
\item{s3_bucket}{Character. Default \code{"cori.data.verse"}.}

\item{s3_prefix}{Character. S3 key prefix. Default \code{"dev/content/"}.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{type}{Character. One of \code{"dataset"}, \code{"chart"}, \code{"package"},
\code{"project"}, \code{"resource"}, \code{"post"}.}
\item{slug}{Character. URL slug (kebab-case directory name).}
\item{location}{Character. Always \code{"remote"} for S3-sourced items.}
\item{local_path}{Character. Always \code{NA} (no local scanning).}
\item{s3_key}{Character. S3 key for the content item.}
}
Sorted by type, then slug. Includes attribute \code{s3_prefix_used}.
}
\description{
Enumerates every content item in the remote S3 bucket, returning an
inventory keyed by type and slug. Use this before slug mapping/validation
so the caller can pick existing slugs by reuse, spot near-matches, and
decide which discovered dependencies will be dangling.
}
