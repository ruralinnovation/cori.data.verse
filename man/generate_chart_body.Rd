% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_chart_qmd.R
\name{generate_chart_body}
\alias{generate_chart_body}
\title{Generate Chart Body Content (Quarto Markdown)}
\usage{
generate_chart_body(
  overview,
  image_file,
  image_alt = NULL,
  source_script = NULL,
  key_findings = character(0),
  data_sources = NULL,
  related_charts = NULL,
  dangling_note = NULL
)
}
\arguments{
\item{overview}{Character. Brief chart overview prose.}

\item{image_file}{Character. Image filename relative to the chart dir
(e.g., \code{"./consumer_sentiment_lc.svg"}). Embedded with markdown image
syntax.}

\item{image_alt}{Character or \code{NULL}. Alt text for the image. Defaults to
the overview if not provided.}

\item{source_script}{Character or \code{NULL}. Relative path to the producing R
script (rendered in a Reproducibility section).}

\item{key_findings}{Character vector. Each element rendered as a bullet.}

\item{data_sources}{\code{data.frame} or \code{NULL}. Columns: \code{slug}, \code{name},
\code{role}, \code{valid} (logical). Rendered as a table with internal links for
valid slugs.}

\item{related_charts}{\code{data.frame} or \code{NULL}. Columns: \code{slug}, \code{name},
\code{valid}. Rendered as a bullet list with internal links for valid slugs.}

\item{dangling_note}{Character or \code{NULL}. Optional auto-generated note
(callout) listing dangling slugs.}
}
\value{
Character string (Quarto markdown).
}
\description{
Produces the Quarto markdown body for a chart's \code{index.qmd}. Embeds the
chart image, optionally lists key findings, links to data sources and
related charts, and includes a reproducibility note pointing to the source
script.
}
